# This is a basic workflow to help you get started with Actions

name: project

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
    paths:
      - 'project.py'
      
  pull_request:
    branches: [ main ]
    paths:
      - 'project.py'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
        with:
          ref: main
          path: ./assignment

      - name: Checkout Tester
        uses: actions/checkout@v4
        with:
          ref: main
          repository: seneca-dsa456/testers
          token: ${{ secrets.TESTER_ACCESS }}
          path: ./tester

      - name: Copy your source files
        run:  cp ./assignment/*.py ./

      - name: Copy data files
        run: cp  ./tester/*.csv ./

      - name: Copy paris data files
        run: cp -r ./tester/paris/ ./

      - name: Copy checkers
        run: cp ./tester/project/* ./

      - name: Run Project - Expand to get timing rubric.
        run: |
          echo "Running project"
          timeout 65 python ./runproject.py

      - name: Check Result - Expand to see MS1/MS2 Rubric Results
        run: |
          echo
          echo "----------------"
          echo
          echo "MS1 Checker:"
          python ./ms1check.py
          echo
          echo "----------------"
          echo
          echo "MS2 Checker:"
          python ./ms2check.py
